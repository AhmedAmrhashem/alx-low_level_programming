#include "main.h"

/**
 * reset_to_98: takes a pointer to an int as parameter and updates the value it points to to 98.
 *@*n: a pointer that that points to an int
 *
 * Return: Always 0
 */
int reset_to_98(int *n)
{
	*n = 98;

	return (0);
}
