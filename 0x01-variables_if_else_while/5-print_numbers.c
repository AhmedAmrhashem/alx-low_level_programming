#include<stdio.h>

/**
 * main - Prints the Alphabet in lowercase
 *
 * Return: Always return 0
 */
int main(void)
{
	int num = 0;

	while (num <= 9)
	{
		printf("%d",num);
		num++;
	}
	putchar('\n');
	return (0);
}
